package ui.mainComponents;

import java.awt.*;

public class mainFrame extends Frame {

    public mainFrame(int Width, int Length, String title)
    {
        this.setTitle(title);
        this.setSize(Width, Length);
    }
}
